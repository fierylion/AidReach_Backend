const Allocation = require('../models/allocation')

// Allocate funds to a specific proposal
const allocateFunds = async (req, res) => {
  // Implementation for allocating funds to a specific proposal
}

// Get allocation details by ID
const getAllocationById = async (req, res) => {
  // Implementation to fetch allocation details by ID
}

// Get all allocations made for a specific proposal
const getAllocationsByProposal = async (req, res) => {
  // Implementation to fetch all allocations made for a specific proposal
}

module.exports = {
  allocateFunds,
  getAllocationById,
  getAllocationsByProposal,
}
